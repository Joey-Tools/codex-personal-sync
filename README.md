# Codex Personal Sync

Release installer, manifest validator, rollback command, and user-level scheduler for Codex configuration packages.

## Test

```bash
python3 -m py_compile scripts/codex_personal_sync.py tests/test_codex_personal_sync.py
python3 -m unittest tests.test_codex_personal_sync
```
