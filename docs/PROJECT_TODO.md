# Project TODO

- [pending] Generate the declared `toolbox` and `private` mirrors from the verified [`sync-source-lock.json`](../sync-source-lock.json), then check and validate each consumer without editing generated files directly.
