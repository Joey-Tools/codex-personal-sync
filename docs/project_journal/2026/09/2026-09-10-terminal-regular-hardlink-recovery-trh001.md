---
id: 20260910-trh001
title: Preserve Terminal Regular Hardlink Authority
status: active
created: 2026-09-10
updated: 2026-09-18
branch: codex/terminal-regular-hardlink-recovery-20260910
pr: https://github.com/Joey-Tools/codex-personal-sync/pull/21
supersedes: []
superseded_by:
---

# 保留终态 Regular Hardlink Authority

## Summary

- 私有 overlay 的独立安全审阅发现，已提交 pending recovery 的终态目标没有持久化允许的 hardlink 数量。
- 若 commit 后出现外来 hardlink，旧逻辑会先建立自己的 recovery alias 并删除 batch 内证据，最后才在终态验证失败；外来路径虽不被删除，但 fail-closed 恢复证据已被过早丢弃。
- 此仓库是同步 engine 及测试的唯一 canonical source，因此在这里修复并刷新 consumer source lock，不能在 Toolbox 或 private overlay 留下反向补丁。

## Decision

- metadata v11 在 commit 前持久化每个终态 regular 目标的精确 link-count；terminal cleanup ticket v8 只接受冻结数量和本 batch 内具名 alias 所能证明的状态。
- alias 创建前和创建/已有后都重新校验精确数量，保护 object identity、content、access policy 与完整的已授权 hardlink 集。
- receipt 写入前会预算完整 terminal validation v3 receipt 的 aliases、祖先目录与 JSON 上限，避免已落盘 ticket/recovery alias 后才发现无法持久化终态 authority。receipt 已持久化后，以一次 identity ledger 同时证明 batch 内仍存 alias 的数量并驱动删除；live target 的 link-count 必须恰为 `1 +` 该 ledger 数量。v3 receipt 为每个 terminal alias 持久化 canonical path、父目录 object identity，以及从 batch root 以下到该 leaf 的全部祖先目录 identity。已消费的 leaf 或目录可以缺失；但任何祖先命名空间一旦同名重现（包括整个 `pending`、self-describing v2 active directory、symlink 或普通目录），都必须在删除前被拒绝。historic v1/v2 receipt 缺少这项 namespace authority，安全地停在明确的 manual-recovery 路径。
- 新写入的、可安全编码的 active subtree token 同时编码 object identity、type 与原 canonical name，恢复时继续按该逻辑路径执行 nofollow、mount、access policy、预算、retained-entry 与 ledger/current equality 校验。writer 以实际目录 fd 的 `NAME_MAX` 约束 v2 token，空间不足时安全回退 v1；decoder 只接受单一可 round-trip 的路径组件。超出 v2 名称上限的历史 content entry 保留 v1 表示并保持 fail-closed；旧式无 canonical-name authority 的 active directory 绝不猜测其子树语义。
- owner-only `0600` 文件的 GID 不影响访问策略，保持为可容忍的良性 metadata churn；不把它错误纳入精确匹配。
- metadata `<11` 的既有 v4/v8 ticket 必须同时绑定当前 batch、finalization marker、retained-representation 准入，以及完整 phase terminal group；v4 逐项比较 identity/content/access，v8 额外比较精确 link-count。historic v1/v2 generic ticket 不含完整 group/count：当 batch root 仍绑定原对象时，metadata I/O/ACL、完整 parser、marker 或 terminal group 失败一律保留原票据并进入稳定的 manual-recovery 路径，绝不把运行时采样的 hardlink 数量误当历史授权。仅当 root identity/binding 已确定被替换，或 metadata 明确声明的 batch-local evidence parent 在 nofollow 探测中呈现 symlink/非目录时，才安全跳过该外来 batch（不删除、不阻塞无关 install）；metadata 缺失与纯 schema-envelope 不兼容保留既有兼容清理契约。新路径也绝不签发 v1/v2。缺失 index 或 ticket 时回落到正常 publisher，安全创建缺失 index 并按当前 legacy-compatible schema 写入，绝不把旧 metadata 的新 ticket 静默升级为 v8。v11 的 unchanged regular target 则强制 `link_count == 1`。

## Evidence and Next Steps

- 新的 deterministic regressions 覆盖 stage、evidence、recovery alias 的真实 unlink 中断和同名 foreign reappearance、alias 父目录及整个 `pending` 已 rmdir 后的重建、伪造 v2 active parent、foreign hardlink、active subtree rename、legacy v1/v2 receipt 的安全人工恢复短路、v6 missing-index/retained-ticket/committed/rollback v4 retry、v6 既有 v8 ticket 的原 bytes/inode 复用与缺失 unchanged target 的 fail-closed，以及伪造的 v11 unchanged count。
- 首轮 frozen whole-range fresh-context review 发现两个新增边界：cleanup action budget 耗尽时，未被扫描的 canonical v1/v2 terminal ticket 可绕过 mutation fence；以及 v1 active-name fallback 在 rename 后崩溃会失去 receipt slot 映射。前者在 mutation fence 重新分类 canonical v1/v2，并只保留已证实 foreign batch 的延期短路；后者只在 parent/object identity 对应唯一未占用 v3 receipt slot 时回绑，否则保持 fail-closed。后续独立审计还证明 synthetic legacy ledger 名可与 receipt alias 同名；因此终态对象的零个或多个候选现在直接拒绝，只有非终态历史对象保留该兼容路径。新增 deterministic zero-budget、foreign-exemption、NAME_MAX fallback crash-recovery、ambiguous-slot 和 synthetic-name-collision regressions。
- 第二轮 frozen fresh-context review 又发现：generic v1/v2 cleanup 已将 batch 内 `pending` 改名为 self-described v2 active token、但 metadata 尚未删除时崩溃，旧 guard 会只从 canonical `pending/**` 重建，导致稳定 manual-recovery 并阻断后续 mutation。恢复现在只接受唯一、绑定 live batch identity、logical name 为 `pending` 的 v2 `0700` directory token；rename 前后分别重验 batch、token pathname/FD、metadata 和 ticket 的 object identity 与 access policy，再回到原有完整 metadata、marker 和 terminal-group fence。该自愈在 mutation gate 中只恢复 canonical 名称，不删除子树；无 token、v1 token、重复 token、替换 token、metadata/ticket 漂移和任何终态 regular group 都保持 fail-closed。新增 v1/v2 retry、name-only mutation-gate recovery、terminal fence、absent/ambiguous/replaced token regressions。
- 最终 fresh review 还指出一个更晚的断点：v1/v2 generic ticket 已完成 metadata、marker 与非终态组检查，但 cleanup walker 删除 `pending/**` 中一个证据后崩溃；下一次旧 parser 会把这个已消费证据误判为人工恢复。现在仅在完整 parser 成功后落专用 `legacy-generic-validation` receipt，冻结 exact ticket、batch/quarantine roots、metadata object/content/access snapshot 与 `pending` directory identity。重试允许这些已消费对象缺失；仍存 canonical 或 active-token 对象必须匹配 receipt，随后 identity ledger 继续拒绝未知、替换或竞争条目。没有此 receipt 的 legacy state 不会获得新短路；receipt 相关 mutation-gate reclassification 仍先验证该边界。新增 v1/v2 marker-consumed retry/mutation-gate 与 post-receipt unknown-entry retention regressions。
- 随后审计补充：仅冻结 metadata 与 `pending` 仍不足以保护同名重建的 marker/evidence slot。receipt 现在在首次完整 parser 和任何 walker mutation 之间，使用排序、全量、受 `MAX_PENDING_CLEANUP_ENTRIES` 与 16 MiB JSON 上限共同约束的 namespace authority，逐项记录 logical path、parent/object identity、type、mode、uid 与 gid；超限在发布前 fail-closed，绝不截断。重试允许已消费条目缺失，但每个仍存 canonical、v2 token 或可唯一回绑的 legacy token 都必须映射至唯一原 slot 并保持 identity/type/access policy；目录 ctime/nlink 不作为变化信号。已消费 marker 后重建同名外部 marker 的 v1/v2 regression 证明 cleanup 与 mutation gate 都保留外部对象、batch 和 ticket。
- 此前原生完整 suite（`1708` tests、`3` expected skips、`1100.208s`）只是 follow-on 修复前的基线。当前 head 先通过静态 ruff、py_compile、diff whitespace、source-lock refresh/check 与相关定向回归；完整 suite 的首次重跑只报告 source-lock 尚未反映新增脚本/测试，刷新 lock 后第二次原生完整 suite 已通过（`1724` tests、`3` expected skips、`1362.295s`）。
- 基线之后的 native frozen review 发现 terminal control retirement 的最终删除边界仍有 TOCTOU：final group 初验后可插入 foreign hardlink，或在 receipt、ticket、empty proof 逐项销毁之间使终态目标重新获得额外链接。新写入的 empty proof 现为 v3，显式冻结 source ticket shape：v4/v8 带完整终态 target group，v5 明确编码无 allocation 的 private-use 已退休形态，v7 则绑定 exact v8 allocation control 及 metadata join。每一个控制文件删除边界都重新比对所需 authority；historic v1/v2 在 exact live ticket 存在时只保留兼容恢复，成为 orphan 后一律保留并要求恢复 exact canonical ticket 或人工恢复。
- 同轮审计也要求 v8 walker 在 `before_isolate` 和 `before_delete` 两个动作边界重新读取 terminal alias ledger；legacy v1/v2 receipt 首次发布前与发布后先重新执行完整 parser/marker 绑定，并在每一个 walker mutation boundary 验证仍存 metadata/marker 的内容与 access policy，防止 receipt 取得后同 inode 内容替换或 marker 替换再借任意无关 entry 删除扩大损失。`0600` owner-only regular file 的 GID churn 继续被视为不改变访问策略的良性变化。
- 随后的 fresh audit 还发现 batch-root 与 orphan proof 的 namespace TOCTOU：proof 发布后，预测性的 isolated batch 路径不能以裸 `rmdir` 删除；而 orphan 或 live proof 退休前也必须反复证明 quarantine root 的 identity、owner-only `0700`/ACL 和 canonical/isolated batch 名仍缺席。普通 terminal batch 现在使用 identity-bound private isolation 再删除；proof 的每个不可逆边界都重新检查该 namespace。新增 v5/v7 orphan proof、v8 batch-root replacement 与 live proof-retirement foreign-batch deterministic regressions 覆盖这些边界。
- rmdir 的实际失败路径也已显式恢复：仅当 private tombstone、原 fd、父目录、空内容与 owner-only policy 仍严格匹配时，才将它原子恢复为 enumerable `isolated_name`，使下一次重试可继续；否则保留证据。empty proof 的最后删除边界还扫描同一 batch identity plan 的 active/retained 私有 root，发现时 fail-closed。新增 inode-bound rmdir-failure retry 与 orphan-proof private-root-residue regressions。
- consumer Toolbox 必须从本 canonical commit 重新生成 receipt，再由 immutable release 进入 private source-lock promotion。
- 针对 checkpoint `c763c17922dc7848bf4322d17d5de4e958d0a0c6` 的 native fresh-context review 又发现三项边界：完整 terminal target group 的合法 v3 empty-proof 可能超过旧 4 KiB 上限；cleanup action budget 耗尽时 canonical/retained orphan empty-proof 未进入 terminal mutation fence；proof 退休的最终目录扫描没有在同一绑定目录快照中同时拒绝新建 canonical/isolated batch root。修复保持 v1/v2 的 4 KiB 兼容上限，只把 v3 提升到 terminal 16 MiB 上限，并在规划期预检完整 v3 envelope、historic v4/v8 删除前精确预检、mutation fence 中拒绝 exact canonical/retained proof；proof root revalidation 以 quarantine root FD 的 identity/binding/access policy 为边界，在初次与最终快照中共同拒绝 canonical、isolated、active 与 retained 表示。`.empty-proof.publish-tmp` 仍被视为未发布暂存，不作为 terminal authority 阻断 mutation。
- 新增 regressions 覆盖 v3 full target-group capacity、capacity preflight 的 no-mutation guarantee、预算耗尽后的 canonical/retained proof fence、publish-temp compatibility，以及 final policy 后重建 canonical/isolated root 的 race。focused tests 5/5 通过；`py_compile`、Ruff、`git diff --check` 和 stock `refresh-lock`/`refresh-lock --check`（11 sources）通过。使用 CI 同款 private-temp runner 的完整 suite 最终通过 `1730` tests、`3` expected skips、`1476.456s`；一次未经 runner 的尝试仅因 sandbox 禁止在账户 home 建立 CI 私有临时目录而产生 4 项 setup errors，另因 lock 尚未刷新产生 1 项预期 stale-lock error，不作为代码回归证据。
- Luna `max` whole-range review 在提交前又发现 legacy v1/v2/v3 ticket 的 batch root 缺失分支：ticket 删除后，empty-proof 删除没有复用最终 root-absence boundary，root 可在竞态中重建而使 authority 被过早退休。修复现在解析 proof authority，并在该不可逆删除边界调用 `_require_pending_cleanup_proof_batch_roots_absent`；新增“ticket 删除后重建 batch root”回归，相关 focused tests `9/9` 通过，静态校验通过。
- 本增量仍需在提交后的精确 frozen range 上使用 GPT-5.6 Luna `max` 完成 final fresh-context review；在该 review、GitHub current-head lane、PR merge 与后续 Toolbox → private immutable release/install 验证完成前，journal 保持 `status: active`。
- Luna `max` final review 随后发现四项边界并已修复：已有 v4 terminal receipt 缺少 recovery-alias namespace authority 时现在明确停在 manual recovery；控制文件删除的 mutation callback 现在在 descriptor/content/ACL/parent checks 之后作为 unlink 前最后一个可观察操作，并覆盖递归 batch walker 的 rmdir/unlink；empty-proof root absence 现在实际 lstat、绑定 parent/object/type/access policy，并扫描 `active-links` root token；ticket、terminal receipt、empty proof、allocation/private-use control 在当前快照及删除边界都拒绝未授权 hardlink。旧 version-1 private-use receipt 仍接受缺省 link-count 字段，但 live object 必须是单链接。新增对应 v4、hardlink、callback 顺序、active-links identity 与 orphan-race regressions；regular overlay 61 tests、pending staging cleanup 112 tests、py_compile、Ruff 和 diff check 均通过。修复后的 head 仍需重新执行 Luna `max` frozen whole-range review。
- 随后的 Luna `max` whole-range review 又发现两项边界并已修复：empty-proof root absence 现在扫描 quarantine root 的全部 no-follow 条目，按预期 batch-root identity/type 识别任意重命名且仍含内容的 root；pending regular staging publication guard、staging cleanup marker 与 ephemeral metadata 改用显式 `require_single_link` policy，reader 与最终 unlink 边界均拒绝外部 hardlink，且 preflight 会在 canonical-to-tombstone rename 前报告 alias。新增四项 deterministic regressions；pending staging cleanup 116 tests、regular overlay 61 tests、regular agent materialization 177 tests、py_compile、Ruff 与 diff check 均通过。当前改动仍需以新 head 重新执行 Luna `max` frozen whole-range review。
- 最新 Luna `max` final review 又发现两项竞态并已修复：legacy v1/v2 无 proof 路径在最终删除 terminal receipt/ticket 前会重复确认 canonical 与 isolated batch root 均不存在；v8 terminal receipt 现在持久化完整 namespace ledger（logical path、parent/object identity、type、mode、uid、gid），重试只允许原条目缺失或保持授权证据，任何非 terminal 条目的新增、替换或访问策略变化均 fail-closed。terminal active-token fallback 仍按唯一 receipt slot 回绑；新增 no-proof root recreation 与 non-terminal recreation regressions。相关模块 117、52、177 tests 通过，且 py_compile、Ruff、diff check 通过；当前 head 仍需重新执行 Luna `max` frozen whole-range review。
- 随后的 Luna `max` review 又发现四项边界并已修复：v8 receipt 的 namespace ledger 不能只靠 receipt 自洽，现由 cleanup ticket 在 commit/rollback 完成后的稳定快照上绑定 SHA-256 锚点；recovery alias、metadata 与 pointer-retirement 自有 state entry 之外的 namespace 改动（包括伪造自洽 receipt）均 fail-closed。完整 namespace receipt 在首次 alias 创建前先做有界容量预检，超限不会留下 recovery alias；walker 每个不可逆动作前再次确认 batch root 的 object identity、mount、canonical binding 与 access policy；legacy empty-proof 的无 proof 路径先确认 quarantine root identity/binding/access policy。新增 self-consistent forged-receipt、capacity no-mutation、walker root replacement regressions；regular finalization 55 tests、pending staging cleanup 117 tests、py_compile、Ruff 与 diff check 均通过。完整 suite、提交后 fresh review、GitHub lane、merge/release/install 仍待完成。
- 上述改动在修正 foreign hardlink 的错误分类后，以 CI 同款 private-temp runner 完整通过 `1744` tests、`3` expected skips（`1585.277s`）；source lock refresh/`--check`、project journal validation、py_compile、Ruff 与 diff check 也通过。提交前仍需在冻结的新 head 上执行 Luna `max` fresh-context whole-range review。
- 提交后的 Luna `max` whole-range review 发现 P2：owner-only `0600` regular 文件的 GID 漂移已被 live access-policy validator 允许，但 terminal namespace anchor 仍把原始 GID 纳入 digest，导致 benign churn 被误判为 authority changed 并长期保留 pending batch。anchor 现在仅对 access policy 不敏感的 regular 文件规范化 GID；receipt 继续保留原始 GID 作为诊断元数据，并新增 GID-drift cleanup regression。相关 regular overlay 模块 `66` tests、py_compile、Ruff 与 diff check 通过；该修复需提交后再次执行同一冻结范围的 Luna `max` review。
- 紧接着的 Luna `max` whole-range review 发现另一个 P2：为兼容旧格式而仍可解析、但缺少 `terminal_namespace_sha256` 的 v8 ticket，会在首次恢复时用较晚的 namespace 快照补发 receipt，进而可能扩大 cleanup 删除授权。解析兼容性保留以便保存原始 ticket/batch，但 `_ensure_pending_terminal_validation_receipt` 现在在无 anchor 时直接进入明确的 manual-recovery 路径，禁止创建 receipt、recovery alias 或删除任何新增条目；新增旧 v8 无 anchor 且注入 `links/*` 的回归。regular overlay 模块 `67` tests、py_compile、Ruff 与 diff check 通过；该修复提交后仍需执行完整 suite 与新的 frozen Luna `max` review。
- source lock 已针对该修复刷新并签名提交；CI 同款 private-temp runner 的第二次完整 suite 通过 `1746` tests、`3` expected skips（`1602.570s`）。第一次全量仅因 lock 尚未刷新而报告 canonical hash mismatch，刷新后 `refresh-lock --check` 与第二次全量均通过。
- 提交后的 frozen Luna `max` review 又发现两项 P2 并已修复：terminal recovery hardlink 创建及 walker 每个 mutation boundary 现在在最后可观察点前后重验 ticket 的 inode、内容、单链接和访问策略；容量规划不再把 `namespace_entries` 当作空集合，而是从完整 pending record、深层 `links/**` backup、claims、publication journals 与 state-retirement entries 投影 namespace，并用最大字段序列化预检，避免合法深层 symlink backup 在 commit 后才因 16 MiB receipt 上限进入 manual recovery。新增同 inode ticket rewrite（hardlink/walker）和深层 namespace capacity regressions；regular overlay `70` tests、pending staging cleanup 与 regular agent materialization 合计 `294` tests、py_compile、Ruff、diff check 通过。该修复需再次刷新 source lock、跑完整 suite 并执行新的 frozen Luna `max` review。
- 修复后的 CI 同款 private-temp runner 完整 suite 通过 `1749` tests、`3` expected skips（`1617.700s`）；source lock refresh/`--check`、project journal validation、py_compile、Ruff 与 diff check 也通过。当前提交 `0df3336` 仍需在精确 frozen range 上执行新的 GPT-5.6 Luna `max` whole-range review，之后才进入 GitHub current-head lane、merge、release、安装与热加载 smoke-test。
- 提交后的 Luna `max` frozen review 发现两项兼容性边界并已修复：v4 terminal ticket 现在和 v8 一样在 walker 的每个不可逆 mutation 前重验 ticket、receipt、batch-root binding、terminal target 与仍存在的 recovery alias；容量投影现在把 before/after 两阶段可能生成的根级 recovery alias 纳入完整 namespace receipt 预检。新增 v4 same-inode ticket rewrite 与 root-alias capacity regressions；两项新 focused tests 已通过，完整 suite、source-lock refresh 与下一轮 frozen review 待执行。
- 修复后的受影响测试族通过 `365` tests；随后 CI 同款 private-temp runner 完整 suite 通过 `1750` tests、`3` expected skips（`1691.708s`）。source lock `refresh-lock --check`、project journal validation、py_compile、Ruff 与 diff check 均再次通过；下一步是对提交后的精确 frozen range 执行 Luna `max` whole-range review。
- 新一轮 GPT-5.6 Luna `max` frozen review 发现两项 P2 并已修复：v4 terminal receipt 缺少完整 namespace authority，现先在 recovery alias 创建后捕获 ledger、再发布通用 receipt，并要求发布前后 ledger 完全一致，避免 receipt 与 ledger 间注入的 foreign batch entry 被当作删除授权；容量 projection 现在依据实际 `state_before.exists` 决定是否计入 `pending/state/before`，避免首次安装把不存在的 evidence 路径错误计入 16 MiB receipt 上限。新增 v4 receipt-publish namespace race 与 no-state-before projection regressions；相关测试族 `366` tests 通过。修复后的 head 仍需刷新 source lock、跑完整 suite 并执行新的 Luna `max` frozen review。
- 上述修复后的 CI 同款 private-temp runner 完整 suite 通过 `1751` tests、`3` expected skips（`1686.052s`）；source lock、journal、静态校验与新的提交后 Luna `max` frozen review 仍是下一步门槛。
- 新 head 的 Luna `max` review 又发现 v4 pre/post ledger 比较把 owner-only regular-file GID 当成严格 authority；现按 `_gid_matches_regular_file_access_policy` 规范化该字段，同时继续严格比较 object identity、type、mode、uid 及敏感 GID。新增 policy regression；受影响测试族 `367` tests 通过（`562.939s`）。该修复仍需刷新 source lock、完整 suite 和新的 frozen review。
- GID policy 修复后的 CI 同款 private-temp runner 完整 suite 通过 `1752` tests、`3` expected skips（`1644.492s`）；source lock、journal validation、py_compile、Ruff 与 diff check 已再次通过。
- GitHub current-head Codex review found a P1 in the v8 namespace anchor: a name-only `state/pending-(complete|publish-error)-…` exclusion could omit a foreign inode from the anchor. Pointer retirement now uses one deterministic batch-bound state path, and the anchor excludes that exact future path only after requiring it to be vacant; every other state child remains bound by its exact namespace entry. Added a regression proving a `state/pending-complete-1-1` replacement changes the anchor digest while the exact protocol path remains excluded.
- Full-suite follow-up exposed one compatibility edge: a newly written v8 ticket could retain its explicit retirement path while a historical test/upgrade downgraded the batch metadata to v6, causing recovery to choose the old dynamic name. v8 tickets now persist the exact retirement path, legacy metadata reuses that path when present, and pre-existing v8 tickets without the field remain on an explicitly isolated legacy-anchor compatibility digest; the old v4/v5 dynamic path behavior remains unchanged. The v6 historical null-GID recovery compatibility family passes again.
- Final CI-matching private-TMP full suite from head `84d5af2` passed: `1753 tests`, `3 expected skips`, `1667.129s`; source lock, journal validation, py_compile, Ruff, and diff check were green before the run. No further source changes are planned for this workstream.
- The fresh GPT-5.6 Luna Max whole-range review found a P1 in the compatibility path for pre-path v8 tickets: when a ticket had no `pointer_retirement_path` and no durable terminal receipt, the legacy name-only digest could ignore a late `state/pending-(complete|publish-error)-...` lookalike and the first recovery could publish a receipt that treated that inode as deletion authority. The compatibility parser remains available for retention, but receipt synthesis now requires exact pointer-retirement authority whenever no durable receipt exists; the path fails closed with manual-recovery guidance before creating recovery aliases or a receipt. Added a crash-window regression that removes the path, injects a late state lookalike, and asserts the ticket, lookalike, and receipt state are retained. The regular-overlay regression module passed `75` tests; source-lock refresh, full-suite rerun, and a new Luna Max review remain pending.
- After that fix, the CI-matching private-temp full suite passed `1754 tests` with `3` expected skips in `2251.924s`; no test, source-lock, journal, compile, lint, or diff-check failures remained.
- The next GPT-5.6 Luna Max whole-range review found two P1 authority gaps. New v8 cleanup tickets now persist exact pointer-retirement and transaction-metadata file identity/content/access evidence and revalidate that shared inode before receipt publication and at every terminal walker mutation boundary, including resumable active-entry tokens. Legacy v4 tickets with terminal regular targets have no durable namespace authority and now stop before alias or receipt creation with explicit manual-recovery guidance; the old pre/post live-ledger path is no longer used as deletion authority. Added regressions for pointer replacement before first receipt, v4 no-authority admission, and compatibility fixtures; regular-overlay finalization passed `76` tests, targeted legacy materialization tests passed, and py_compile/Ruff were green. The updated source still needs lock refresh, full-suite validation, a new frozen Luna Max review, and the GitHub/merge/release/install gates.
- Follow-up compatibility work preserves automatic recovery for newly written legacy v4 tickets without rebinding historic authority. New v4 tickets now persist the terminal namespace anchor, deterministic pointer-retirement path and shared pointer/metadata evidence, derive action-scoped hard-link counts when older metadata omits them, and use the same complete alias/namespace receipt and mutation-fence protocol as v8. Historic v4/v8 tickets missing those fields, or tickets whose already-published authority is later rewritten, remain explicitly manual-recovery/fail-closed. This preserves old v6/v7 writer recovery while rejecting post-ticket metadata mutation; the mixed historical null-GID committed case is covered by that manual-recovery regression.
- The first post-`b38d678` full CI-matching run exposed `12` compatibility errors in `1756` tests because all legacy v4 terminal cleanup was deferred. The v4 authority redesign removed those errors; focused compatibility and receipt tests now pass (`11` targeted tests), and static checks plus source-lock refresh/`--check` pass. A subsequent related-module run reached `281` tests with one action-scoped v4 fixture failure; the fixture-specific derivation bug was corrected and its regression now passes. The complete suite must still be rerun from this final source state before commit/review.
- The final-source related module run passed `398` tests. The subsequent CI-matching private-temp full suite completed `1756` tests with `3` expected skips and one deterministic `test_repository_source_lock_is_current` error because this last marker-only v4 compatibility guard had not yet been reflected in the lock. The lock was then refreshed and independently verified for all `11` sources; the source-lock test, py_compile, Ruff, diff check, and journal validation all pass. No new functional test failure was reported after the guard; the branch still requires the post-lock frozen Luna Max review before commit.
- The next native GPT-5.6 Luna Max whole-range review found two terminal control-authority gaps: receipt synthesis could continue when only one of pointer-retirement or transaction-metadata names remained, and the recorded shared-inode hard-link count was not enforced at pointer clear or cleanup boundaries. Cleanup now stops one-sided authority before receipt creation, pointer quarantine rechecks the expected count at both sides of the move, and receipt-backed cleanup allows only the one explicit protocol-owned active-token transition while rejecting extra aliases. New regressions cover one-sided authority and pointer-retirement hard-link aliases; the affected pending/regular suite passes `400` tests, and static, source-lock, and journal validation are green. The new commit still requires a fresh frozen Luna Max review and the GitHub/merge/release/install gates.
- The post-fix CI-matching private-TMP full suite then passed `1758` tests with `3` expected skips in `2271.991s`; no test errors or failures were reported. This exact final source state is now ready for the last Luna Max whole-range review, GitHub current-head lane, merge, release, installation, and hot-load smoke test.
